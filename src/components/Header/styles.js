import styled from 'styled-components';

export const MainHeader = styled.header`
    background: var(--red);
    img{
        width: 40px;
    }
    a{
        cursor: pointer;
        font-size: 1.2rem;
        font-weight: 600;
        color: var(--white);
    }
`